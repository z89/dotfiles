# GTK_THEME pins every GTK app, libadwaita ones included, to the theme-apply generated theme so
# the Matugen-A/B gtk-theme flip re-reads the palette live. See ~/.local/bin/theme-apply (step_gtk3).
GTK_THEME=Matugen-A
