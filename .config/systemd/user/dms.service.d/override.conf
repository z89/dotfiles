# Run DMS through the patched-shell wrapper (wider Spotlight launcher).
# See ~/.local/bin/dms-run-patched and ~/.local/bin/dms-shell-patch.
[Service]
ExecStart=
ExecStart=%h/.local/bin/dms-run-patched
